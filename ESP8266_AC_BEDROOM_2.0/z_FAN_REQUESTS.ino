uint32_t * FANREQUEST_HIGH() {
  int indval = 1;
  uint32_t * SPEED;
  if (indexArray == indval) {
    SPEED = FHH62;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FHH64;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FHH65;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FHH66;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FHH67;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FHH68;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FHH69;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FHH70;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FHH71;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FHH72;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FHH73;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FHH74;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FHH75;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FHH76;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FHH72;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FHH77;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FHH78;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FHH79;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FHH80;
  }
  /* indval++;
  if (indexArray == indval) {
    SPEED = FHH82;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FHH83;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FHH84;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FHH85;
  }
  */

 return SPEED;
  
}

uint32_t * FANREQUEST_MED() {

  int indval = 1;
  uint32_t * SPEED;
    /*
  if (indexArray == indval) {
    SPEED = FMH62;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FMH64;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FMH65;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FMH66;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FMH67;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FMH68;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FMH69;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FMH70;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FMH71;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FMH72;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FMH73;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FMH74;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FMH75;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FMH76;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FMH72;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FMH77;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FMH78;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FMH79;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FMH80;
  }
  /* indval++;
  if (indexArray == indval) {
    SPEED = FMH82;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FMH83;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FMH84;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FMH85;
  }
  
*/
 return SPEED;
  
}

uint32_t * FANREQUEST_LOW() {
 
  int indval = 1;
  uint32_t * SPEED;
   /*
  if (indexArray == indval) {
    SPEED = FLH62;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FLH64;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FLH65;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FLH66;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FLH67;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FLH68;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FLH69;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FLH70;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FLH71;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FLH72;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FLH73;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FLH74;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FLH75;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FLH76;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FLH72;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FLH77;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FLH78;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FLH79;
  }
   indval++;
  if (indexArray == indval) {
    SPEED = FLH80;
  }
  /* indval++;
  if (indexArray == indval) {
    SPEED = FLH82;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FLH83;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FLH84;
  }
  indval++;
  if (indexArray == indval) {
    SPEED = FLH85;
  }
  */

 return SPEED;
  
}
